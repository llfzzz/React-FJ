/* Free Joy — EmptyState.css
   Pairs with EmptyState.tsx / EmptyState.jsx. Requires the FJ tokens (tokens/*.css).
   Pass an inline <svg> (Lucide or your own) as the icon.
   .fj-empty--compact tightens the padding. */

.fj-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  gap: 6px;
  padding: 56px 32px;
}
.fj-empty--compact { padding: 32px 24px; }

.fj-empty-badge {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 64px;
  height: 64px;
  margin-bottom: 8px;
  border-radius: var(--radius-pill);
  background: var(--paper-2);
}
.fj-empty--compact .fj-empty-badge { width: 48px; height: 48px; }

.fj-empty-icon {
  display: inline-flex;
  color: var(--text-subtle);
}
.fj-empty--compact .fj-empty-icon svg { width: 22px; height: 22px; }

.fj-empty-title {
  font-family: var(--font-display);
  font-size: var(--text-lg);
  font-weight: var(--weight-semibold);
  color: var(--text);
}

.fj-empty-text {
  font-size: var(--text-sm);
  color: var(--text-muted);
  max-width: 340px;
  line-height: 1.5;
}

.fj-empty-action { margin-top: 14px; }
