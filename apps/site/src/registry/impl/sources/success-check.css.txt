<!-- Free Joy — SuccessCheck (effect), no-framework version.
     Requires the FJ tokens (tokens/*.css).
     Pure CSS: the checkmark path draws itself in via stroke-dashoffset, and a
     soft ring pulses out once. Plays on load here; to trigger later, add the
     .is-drawn class from JS. -->

<span class="fj-check is-drawn" role="img" aria-label="Success" style="--fj-size: 48px">
  <span class="fj-check-ring" aria-hidden="true"></span>
  <svg viewBox="0 0 52 52" aria-hidden="true">
    <circle class="fj-check-circle" cx="26" cy="26" r="24" />
    <path class="fj-check-mark" d="M14 27 L23 35 L38 18" />
  </svg>
</span>

<style>
.fj-check {
  --fj-size: 48px;
  --fj-color: var(--success-500);
  --fj-duration: 500ms;

  position: relative;
  display: inline-flex;
  width: var(--fj-size);
  height: var(--fj-size);
  color: var(--fj-color);
}

.fj-check svg { width: 100%; height: 100%; }

.fj-check-circle {
  fill: none;
  stroke: currentColor;
  stroke-opacity: 0.2;
  stroke-width: 2;
}

.fj-check-mark {
  fill: none;
  stroke: currentColor;
  stroke-width: 3.5;
  stroke-linecap: round;
  stroke-linejoin: round;
  stroke-dasharray: 28;
  stroke-dashoffset: 28;
  transition: stroke-dashoffset var(--fj-duration) var(--ease-out);
}

.fj-check.is-drawn .fj-check-mark { stroke-dashoffset: 0; }

.fj-check-ring {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  border: 2px solid currentColor;
  opacity: 0;
}
.fj-check.is-drawn .fj-check-ring {
  animation: fj-check-ring 700ms var(--ease-out) forwards;
}

@keyframes fj-check-ring {
  from { transform: scale(0.7); opacity: 0.6; }
  to   { transform: scale(1.15); opacity: 0; }
}

@media (prefers-reduced-motion: reduce) {
  .fj-check-mark { transition: none; stroke-dashoffset: 0; }
  .fj-check-ring { display: none; }
}
</style>
