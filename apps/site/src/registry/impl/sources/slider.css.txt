/* Free Joy — Slider.css
   Pairs with Slider.tsx / Slider.jsx. Requires the FJ tokens (tokens/*.css).
   The component sets --fj-pct (fill width, 0–100%) and --fj-thumb (accent
   color) inline; the transparent native range sits on top so the thumb
   stays draggable. */

.fj-slider {
  display: flex;
  align-items: center;
  gap: 14px;
  width: 100%;
}

.fj-slider-rail {
  position: relative;
  flex: 1;
  height: 22px;
  display: flex;
  align-items: center;
}

.fj-slider-track {
  position: absolute;
  inset: auto 0;
  height: 6px;
  border-radius: 999px;
  background: var(--paper-3);
}

.fj-slider-fill {
  position: absolute;
  left: 0;
  height: 6px;
  border-radius: 999px;
  width: var(--fj-pct, 50%);
  background: var(--fj-thumb, var(--joy-500));
}

/* Transparent native range sits on top so the thumb stays draggable. */
.fj-slider-input {
  position: absolute;
  inset: 0;
  width: 100%;
  margin: 0;
  appearance: none;
  -webkit-appearance: none;
  background: transparent;
  cursor: pointer;
}
.fj-slider-input:disabled { cursor: not-allowed; }

.fj-slider-input::-webkit-slider-thumb {
  -webkit-appearance: none;
  width: 20px;
  height: 20px;
  border-radius: 50%;
  background: var(--fj-thumb, var(--joy-500));
  border: 3px solid var(--surface);
  box-shadow: var(--shadow-sm);
  cursor: pointer;
}
.fj-slider-input::-moz-range-thumb {
  width: 20px;
  height: 20px;
  border: 3px solid var(--surface);
  border-radius: 50%;
  background: var(--fj-thumb, var(--joy-500));
  box-shadow: var(--shadow-sm);
  cursor: pointer;
}

.fj-slider-input:focus-visible::-webkit-slider-thumb {
  outline: 3px solid var(--accent);
  outline-offset: 2px;
}

.fj-slider-value {
  min-width: 36px;
  text-align: right;
  font-family: var(--font-mono);
  font-size: var(--text-sm);
  color: var(--text-muted);
}

.fj-slider:has(.fj-slider-input:disabled) { opacity: 0.5; }
