<!-- Free Joy — Modal (Dialog), no-framework version.
     Requires the FJ tokens (tokens/*.css).
     Toggle the .is-open class to show/hide. Focus-trapping, Escape-to-close and
     backdrop-click are behavior you wire in JS — the styles + entrance are here.
     Tip: the native <dialog> element gives you the focus trap + Esc for free. -->

<div class="fj-modal is-open" role="presentation">
  <div class="fj-modal-panel" role="dialog" aria-modal="true" aria-label="Rename mix" tabindex="-1">
    <div class="fj-modal-head">
      <h3 class="fj-modal-title">Rename mix</h3>
      <button class="fj-modal-close" aria-label="Close">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
      </button>
    </div>
    <div class="fj-modal-body">Give this set a name your followers will recognise.</div>
    <div class="fj-modal-foot">
      <button class="fj-btn fj-btn--secondary fj-btn--md">Cancel</button>
      <button class="fj-btn fj-btn--primary fj-btn--md">Save</button>
    </div>
  </div>
</div>

<style>
.fj-modal {
  position: fixed;
  inset: 0;
  z-index: var(--z-modal);
  display: none;
  align-items: center;
  justify-content: center;
  padding: 20px;
  background: var(--overlay);
  -webkit-backdrop-filter: blur(2px);
  backdrop-filter: blur(2px);
}
.fj-modal.is-open {
  display: flex;
  animation: fj-modal-in var(--dur-base) var(--ease-out) both;
}

.fj-modal-panel {
  width: min(100%, 460px);
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-lg);
  overflow: hidden;
  animation: fj-modal-panel var(--dur-base) var(--ease-out) both;
}

/* Glass variant */
.fj-modal-panel--glass {
  background: var(--glass-bg-strong);
  border-color: var(--glass-border);
  -webkit-backdrop-filter: blur(var(--glass-blur-lg)) saturate(180%);
  backdrop-filter: blur(var(--glass-blur-lg)) saturate(180%);
}

.fj-modal-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 18px 20px 0;
}
.fj-modal-title {
  margin: 0;
  font-size: var(--text-lg);
  font-weight: var(--weight-semibold);
  color: var(--text);
}
.fj-modal-close {
  margin-left: auto;
  display: inline-flex;
  padding: 4px;
  border: none;
  background: transparent;
  color: var(--text-subtle);
  cursor: pointer;
}
.fj-modal-close:hover { color: var(--text); }

.fj-modal-body {
  padding: 14px 20px 20px;
  color: var(--text-muted);
  font-size: var(--text-base);
  line-height: var(--leading-normal);
}
.fj-modal-foot {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  padding: 0 20px 20px;
}

@keyframes fj-modal-in { from { opacity: 0; } to { opacity: 1; } }
@keyframes fj-modal-panel {
  from { opacity: 0; transform: translateY(8px) scale(0.98); }
  to   { opacity: 1; transform: none; }
}

@media (prefers-reduced-motion: reduce) {
  .fj-modal.is-open, .fj-modal-panel { animation-duration: 0.01ms; }
}
</style>
