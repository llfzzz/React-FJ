<!-- Free Joy — LoaderDots (effect), no-framework version.
     Requires the FJ tokens (tokens/*.css).
     Three dots pulse in sequence via staggered animation-delay. Reduced motion
     holds them at a steady mid-opacity. -->

<span class="fj-dots" role="status" aria-label="Loading" style="--fj-size: 8px; --fj-color: var(--accent)">
  <span class="fj-dots-dot" aria-hidden="true"></span>
  <span class="fj-dots-dot" aria-hidden="true"></span>
  <span class="fj-dots-dot" aria-hidden="true"></span>
</span>

<style>
.fj-dots {
  --fj-size: 8px;
  --fj-color: var(--accent);
  --fj-duration: 1400ms;

  display: inline-flex;
  align-items: center;
  gap: 6px;
}

.fj-dots-dot {
  width: var(--fj-size);
  height: var(--fj-size);
  border-radius: 50%;
  background: var(--fj-color);
  animation: fj-loader-dots var(--fj-duration) var(--ease-in-out) infinite;
}
.fj-dots-dot:nth-child(2) { animation-delay: calc(var(--fj-duration) / 6); }
.fj-dots-dot:nth-child(3) { animation-delay: calc(var(--fj-duration) / 3); }

@keyframes fj-loader-dots {
  0%, 80%, 100% { transform: scale(0.6); opacity: 0.4; }
  40% { transform: scale(1); opacity: 1; }
}

@media (prefers-reduced-motion: reduce) {
  .fj-dots-dot { animation: none; opacity: 0.7; }
}
</style>
