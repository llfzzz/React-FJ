<!-- Free Joy — Progress, no-framework version.
     Requires the FJ tokens (tokens/*.css).
     Determinate: set --fj-pct. Indeterminate: add .fj-progress--indeterminate.
     Sizes: sm | md (default) | lg. -->

<!-- Determinate -->
<div class="fj-progress" style="--fj-pct: 68%; --fj-fill: var(--joy-500)">
  <div class="fj-progress-track" role="progressbar" aria-valuenow="68" aria-valuemin="0" aria-valuemax="100">
    <div class="fj-progress-bar"></div>
  </div>
  <span class="fj-progress-label">68%</span>
</div>

<!-- Indeterminate -->
<div class="fj-progress fj-progress--indeterminate" style="--fj-fill: var(--joy-500)">
  <div class="fj-progress-track" role="progressbar">
    <div class="fj-progress-bar"></div>
  </div>
</div>

<style>
.fj-progress {
  display: flex;
  align-items: center;
  gap: 12px;
  width: 100%;
}

.fj-progress-track {
  position: relative;
  flex: 1;
  height: 8px;
  border-radius: 999px;
  background: var(--paper-3);
  overflow: hidden;
}
.fj-progress--sm .fj-progress-track { height: 5px; }
.fj-progress--lg .fj-progress-track { height: 12px; }

.fj-progress-bar {
  position: absolute;
  top: 0;
  bottom: 0;
  left: 0;
  width: var(--fj-pct, 0%);
  border-radius: 999px;
  background: var(--fj-fill, var(--joy-500));
  transition: width var(--dur-base) var(--ease-out);
}

/* Indeterminate: a 40%-wide bar sweeps across on a loop. */
.fj-progress--indeterminate .fj-progress-bar {
  width: 40%;
  transition: none;
  animation: fj-prog 1.2s var(--ease-in-out) infinite;
}

.fj-progress-label {
  min-width: 38px;
  text-align: right;
  font-family: var(--font-mono);
  font-size: var(--text-sm);
  color: var(--text-muted);
}

@keyframes fj-prog {
  0%   { left: -40%; }
  100% { left: 100%; }
}

@media (prefers-reduced-motion: reduce) {
  .fj-progress-bar { transition-duration: 0.01ms; }
  .fj-progress--indeterminate .fj-progress-bar { animation: none; left: 0; width: 100%; opacity: 0.5; }
}
</style>
