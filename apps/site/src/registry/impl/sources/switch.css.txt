/* Free Joy — Switch.css
   Pairs with Switch.tsx / Switch.jsx. Requires the FJ tokens (tokens/*.css).
   Native checkbox is visually hidden; :checked slides the thumb + tints track. */

.fj-switch {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  cursor: pointer;
}

.fj-switch-input {
  position: absolute;
  width: 1px;
  height: 1px;
  opacity: 0;
}

.fj-switch-track {
  position: relative;
  width: 44px;
  height: 26px;
  flex: none;
  border-radius: var(--radius-pill);
  background: var(--paper-3);
  transition: background var(--dur-base) var(--ease-out);
}

.fj-switch-thumb {
  position: absolute;
  top: 3px;
  left: 3px;
  width: 20px;
  height: 20px;
  border-radius: 50%;
  background: var(--white);
  box-shadow: var(--shadow-sm);
  transition: left var(--dur-base) var(--ease-out);
}

.fj-switch-input:checked + .fj-switch-track { background: var(--accent); }
.fj-switch-input:checked + .fj-switch-track .fj-switch-thumb { left: 21px; }

.fj-switch-input:focus-visible + .fj-switch-track {
  outline: 3px solid var(--accent);
  outline-offset: 2px;
}

.fj-switch-input:disabled ~ * { opacity: 0.5; }
.fj-switch:has(.fj-switch-input:disabled) { cursor: not-allowed; }

.fj-switch-label {
  font-size: var(--text-base);
  color: var(--text);
}

@media (prefers-reduced-motion: reduce) {
  .fj-switch-track, .fj-switch-thumb { transition-duration: 0.01ms; }
}
