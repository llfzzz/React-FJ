/* Free Joy — Spinner.css
   Pairs with Spinner.tsx / Spinner.jsx. Requires the FJ tokens (tokens/*.css).
   Size, color, and stroke width arrive via custom properties set inline. */

.fj-spinner {
  display: inline-flex;
  align-items: center;
  gap: 9px;
  color: var(--fj-spinner-color, var(--accent));
}

.fj-spinner-ring {
  width: var(--fj-spinner-size, 22px);
  height: var(--fj-spinner-size, 22px);
  flex: none;
  border-radius: 50%;
  border: var(--fj-spinner-thickness, 2.5px) solid currentColor;
  border-top-color: transparent;
  opacity: 0.9;
  animation: fj-spin 0.7s linear infinite;
}

.fj-spinner-label {
  font-size: var(--text-sm);
  color: var(--text-muted);
}

@keyframes fj-spin { to { transform: rotate(360deg); } }

/* Reduced motion: slow it right down rather than freeze (still conveys "busy"). */
@media (prefers-reduced-motion: reduce) {
  .fj-spinner-ring { animation-duration: 2.4s; }
}
