<!-- Free Joy — Spinner, no-framework version.
     Requires the FJ tokens (tokens/*.css).
     Size + color are controlled with custom properties. -->

<span class="fj-spinner" role="status" aria-label="Loading" style="--fj-spinner-size: 22px; --fj-spinner-color: var(--accent)">
  <span class="fj-spinner-ring"></span>
</span>

<!-- With label -->
<span class="fj-spinner" role="status" style="--fj-spinner-size: 18px">
  <span class="fj-spinner-ring"></span>
  <span class="fj-spinner-label">Loading mixes…</span>
</span>

<style>
.fj-spinner {
  display: inline-flex;
  align-items: center;
  gap: 9px;
  color: var(--fj-spinner-color, var(--accent));
}

.fj-spinner-ring {
  width: var(--fj-spinner-size, 22px);
  height: var(--fj-spinner-size, 22px);
  flex: none;
  border-radius: 50%;
  border: 2.5px solid currentColor;
  border-top-color: transparent;
  opacity: 0.9;
  animation: fj-spin 0.7s linear infinite;
}

.fj-spinner-label {
  font-size: var(--text-sm);
  color: var(--text-muted);
}

@keyframes fj-spin { to { transform: rotate(360deg); } }

/* Reduced motion: slow it right down rather than freeze (still conveys "busy"). */
@media (prefers-reduced-motion: reduce) {
  .fj-spinner-ring { animation-duration: 2.4s; }
}
</style>
