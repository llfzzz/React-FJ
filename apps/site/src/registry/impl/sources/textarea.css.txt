/* Free Joy — Textarea.css
   Pairs with Textarea.tsx / Textarea.jsx. Requires the FJ tokens (tokens/*.css),
   including --ring for the focus glow.
   Matches the Input field styling; error state via .fj-field--error. */

.fj-field {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.fj-field-label {
  font-size: var(--text-sm);
  font-weight: var(--weight-medium);
  color: var(--text-muted);
}

.fj-textarea {
  width: 100%;
  padding: 11px 14px;
  background: var(--surface);
  border: 1px solid var(--border-strong);
  border-radius: var(--radius-md);
  font-family: var(--font-text);
  font-size: var(--text-base);
  line-height: var(--leading-normal);
  color: var(--text);
  outline: none;
  resize: vertical;
  transition:
    border-color var(--dur-fast) var(--ease-out),
    box-shadow var(--dur-fast) var(--ease-out);
}

.fj-textarea:focus {
  border-color: var(--accent);
  box-shadow: var(--ring);
}

.fj-field-note {
  font-size: var(--text-xs);
  color: var(--text-subtle);
}

.fj-field--error .fj-textarea,
.fj-field--error .fj-textarea:focus {
  border-color: var(--danger-500);
}
.fj-field--error .fj-field-note { color: var(--danger-700); }

@media (prefers-reduced-motion: reduce) {
  .fj-textarea { transition-duration: 0.01ms; }
}
