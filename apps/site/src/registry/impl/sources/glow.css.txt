/* Free Joy — Glow.css
   Pairs with Glow.tsx / Glow.jsx. Requires the FJ tokens (tokens/*.css).
   Static, single blur layer behind the child. The component feeds
   --fj-glow-colors, --fj-blur, --fj-intensity, and --fj-spread. No animation. */

.fj-glow {
  position: relative;
  display: inline-block;
}

.fj-glow-aura {
  position: absolute;
  inset: calc(-1 * var(--fj-spread, 8px));
  background: linear-gradient(120deg, var(--fj-glow-colors, var(--joy-300), var(--bloom-300)));
  filter: blur(var(--fj-blur, 48px));
  opacity: var(--fj-intensity, 0.35);
  border-radius: var(--radius-xl);
  pointer-events: none;
}

.fj-glow-content { position: relative; }
